@model Event
@{
    ViewData["Title"] = "Delete";
}

<div class="card">
    <div class="card-body">
        <h4 class="card-title">@Model.Title</h4>
        <p class="card-title-desc">@Model.Date</p>
        <p class="card-title-desc">@Model.Month</p>
        <p class="card-title-desc">@Model.Start</p>
        <p class="card-title-desc">@Model.End</p>
        <p class="card-title-desc">@Model.Location</p>
        <div class="d-flex flex-wrap gap-2">
            <form method="post" asp-route-id="@Model.Id">
                <button type="submit" class="btn btn-danger waves-effect waves-light">Delete</button>
            </form>
            <a asp-action="Index" class="btn btn-light waves-effect">Back to List</a>
        </div>
    </div>
</div>